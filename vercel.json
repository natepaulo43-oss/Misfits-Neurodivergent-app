{
  "version": 2,
  "installCommand": "npm install --prefix misfits",
  "buildCommand": "npm run vercel-build --prefix misfits",
  "outputDirectory": "misfits/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

'use strict';

// Minimal mock for react-native used only in unit tests.
// ImageSourcePropType (and all other RN exports) are TypeScript types —
// no runtime values are needed for the matching-logic tests.
module.exports = {};
